<odoo>

    <record id="hr_hospital_desease_report_wizard_view_form" model="ir.ui.view">
        <field name="name">hr.hospital.desease.report.wizard.view.form</field>
        <field name="model">hr.hospital.desease.report.wizard</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <label for="doctor_ids" string="Doctors"/>
                        <field name="doctor_ids" nolabel="1" widget="many2many_tags"/>

                        <label for="desease_ids" string="Deseases"/>
                        <field name="desease_ids" nolabel="1" widget="many2many_tags"/><group>
                            <field name="date_from" />
                            <field name="date_to" />
                        </group>
                    </group>
                    <footer>
                        <button name="action_generate_report"
                                string="Generate report"
                                type="object"
                                class="btn-primary"/>
                        <button string="Cancle" class="btn-secondary" special="cancel"/>
                    </footer>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_hr_hospital_desease_report_wizard" model="ir.actions.act_window">
        <field name="name">Desease report</field>
        <field name="res_model">hr.hospital.desease.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="hr_hospital.model_hr_hospital_doctor"/>
        <field name="binding_view_types">list,form</field>
        <field name="binding_type">report</field>
    </record>

</odoo>